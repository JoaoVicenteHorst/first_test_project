# Database Configuration
# PostgreSQL (Production)
DATABASE_URL="postgresql://user:password@localhost:5432/user_management?schema=public"

# SQLite (Development) - Uncomment to use SQLite instead
# DATABASE_URL="file:./dev.db"

# Server Configuration
PORT=5000
NODE_ENV="development"

# JWT Secret Key (IMPORTANT: Change this in production!)
JWT_SECRET="your-secret-key-change-in-production"

# CORS Configuration (Production)
# IMPORTANT FOR RENDER: Set this to your exact frontend URL
# Example: FRONTEND_URL="https://my-app.onrender.com"
# - Use https:// (Render uses HTTPS)
# - No trailing slash
# - Must match exactly what browser sends
FRONTEND_URL="https://your-frontend.onrender.com"

# Optional: Additional custom origin (staging, custom domain, etc.)
# CORS_ORIGIN="https://staging.myapp.com"

# Frontend API URL (optional)
# Only needed if backend runs on different port/host
# VITE_API_URL="http://localhost:5000/api"

